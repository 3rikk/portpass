{
  "name": "portpass",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --config wrangler.jsonc",
    "deploy:worker": "wrangler deploy --config wrangler.jsonc",
    "deploy:pages": "wrangler pages deploy . --project-name=portpass --config wrangler.pages.jsonc",
    "test": "node tests/rules.test.js"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}